/** Defina como `false` quando quiser publicar o site antes da data ou manter só o site completo. */
export const ENABLE_LAUNCH_SCREEN = true;

/** 20 de abril de 2026, 13:00 — usa o fuso horário local do navegador. */
export const LAUNCH_AT_MS = new Date(2026, 3, 20, 13, 0, 0).getTime();
